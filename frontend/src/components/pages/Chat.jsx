import ChannelsForm from '../ChannelList';

const Chat = () => (
  <div className="h-100">
    <div className="d-flex flex-column h-100">
      <ChannelsForm />
    </div>
  </div>
);

export default Chat;
